import LOADER from './loader';

const validationList = {
  GET_CONTENT_HASHES: LOADER.GET_CONTENT_HASHES,
  NEW_DOCUMENT: LOADER.NEW_DOCUMENT,
  SET_TEXT: LOADER.SET_TEXT,
};

export default validationList;
